# HTML notes
# CSS notes
# JavaScript notes
# Bugs I Fixed
- Fixed a misspelled `script.js` file name. The JavaScript did not work because the HTML could not find the file.
- Fixed a missing quote in the input element: `type="text"`.
- Removed an accidental extra backtick from the HTML page.
- Updated `resetText()` so it resets to the correct title.
# things i learned
   01/05/26
- HTML creates the structure of a webpage.
- CSS controls how the page looks.
- JavaScript controls behavior and interactivity.
- A `<div>` is a container used to group elements.
- Git saves versions of my code.
- GitHub stores my code online.
- GitHub Pages turns my project into a live website.
